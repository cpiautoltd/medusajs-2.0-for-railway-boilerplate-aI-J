import { MedusaService } from "@medusajs/framework/utils"
import ExtrudedProducts from "./models/extruded-products"

class ExtrudedProductsModuleService extends MedusaService({
  ExtrudedProducts,
}){

  
}

export default ExtrudedProductsModuleService